{
  "name": "appium-live-view-plugin",
  "version": "0.1.3",
  "description": "Appium plugin that renders the active session's page source + screenshot into a single self-contained, interactive HTML live view (hover to highlight, click to inspect element attributes and locators), ready to attach to Allure or any report.",
  "type": "module",
  "main": "index.js",
  "appium": {
    "pluginName": "liveView",
    "mainClass": "LiveViewPlugin"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@appium/base-plugin": "^3.3.2"
  },
  "peerDependencies": {
    "appium": "^2.0.0 || ^3.0.0"
  },
  "peerDependenciesMeta": {
    "appium": {
      "optional": true
    }
  },
  "files": [
    "index.js",
    "lib",
    "README.md"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
