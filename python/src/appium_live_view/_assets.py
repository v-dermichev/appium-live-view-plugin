# Auto-generated by tools/extract-assets.mjs — do not edit by hand.
# The browser CSS and runtime JS from lib/render.js, shared verbatim by the
# JS and Python renderers. Re-run `node tools/extract-assets.mjs` after editing
# the <style> or <script> in lib/render.js.

BASE_CSS = "\n:root{color-scheme:light dark}\n*{box-sizing:border-box}\nbody{margin:0;font:13px/1.45 -apple-system,BlinkMacSystemFont,\"Segoe UI\",Roboto,sans-serif;color:#1c1c1e;background:#f5f5f7}\n/* position:fixed (not absolute) so checking a hidden radio via a label click\n   doesn't scroll the page to the radio at the top — the view stays put. */\n.lv-r{position:fixed;top:0;left:0;width:0;height:0;opacity:0;pointer-events:none}\n.lv-head{display:flex;gap:12px;align-items:baseline;padding:10px 14px;border-bottom:1px solid rgba(0,0,0,.1)}\n.lv-head h1{font-size:14px;margin:0;font-weight:600}\n.lv-head .lv-meta{color:#6e6e73;font-size:12px}\n.lv-head .lv-testers{margin-left:auto;display:flex;align-items:center;gap:10px;flex-wrap:wrap;justify-content:flex-end}\n.lv-head .lv-tester{display:flex;align-items:center;gap:8px}\n.lv-head input[type=text]{font:inherit;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12px;width:min(300px,44vw);padding:4px 8px;border:1px solid rgba(0,0,0,.2);border-radius:6px;background:#fff;color:inherit}\n.lv-head .lv-strat{font:inherit;font-size:12px;padding:4px 6px;border:1px solid rgba(0,0,0,.2);border-radius:6px;background:#fff;color:inherit;cursor:pointer}\n.lv-xpath-status{font-size:12px;white-space:nowrap;color:#8a8a8e}\n.lv-xpath-status.ok{color:#16a34a}\n.lv-xpath-status.warn{color:#d97706}\n.lv-xpath-status.err{color:#e5484d}\n.lv-head .lv-tools{display:flex;gap:6px}\n.lv-btn{font:inherit;font-size:12px;padding:4px 10px;border:1px solid rgba(0,0,0,.2);border-radius:6px;background:#fff;color:inherit;cursor:pointer;white-space:nowrap;text-decoration:none;display:inline-block}\n.lv-btn:hover{background:#eee}\na.lv-btn:not([href]){opacity:.5;cursor:default}\n.lv-tree{display:none;flex:1 1 300px;min-width:240px;max-height:78vh;overflow:auto;background:#fff;border:1px solid rgba(0,0,0,.1);border-radius:10px}\n#lv-tree-toggle:checked~.lv-main .lv-tree{display:block}\n#lv-tree-toggle:checked~.lv-head .lv-btn-src{background:#e5e5ea;border-color:rgba(0,0,0,.4)}\n.lv-tree-head{position:sticky;top:0;background:#fff;padding:7px 10px;border-bottom:1px solid rgba(0,0,0,.08);font-size:11px;text-transform:uppercase;letter-spacing:.04em;color:#8a8a8e}\n.lv-tree-body{padding:4px 2px}\n.lv-node{display:block;white-space:nowrap;padding:2px 6px;border-radius:4px;cursor:pointer;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12px;color:inherit}\n.lv-node:hover{background:rgba(59,130,246,.14)}\n.lv-node.lv-node-hit{background:rgba(168,85,247,.22)}\n.lv-node-tag{font-weight:600}\n.lv-node-attr{color:#8a8a8e}\n.lv-node-nobox{color:#c0392b;opacity:.7}\n.lv-main{display:flex;gap:16px;align-items:flex-start;padding:14px;flex-wrap:wrap}\n.lv-stagecol{display:flex;flex-direction:column;gap:10px;flex:0 0 auto;width:min(420px,92vw)}\n.lv-hint{display:none;color:#6e6e73;background:#fff;border:1px solid rgba(0,0,0,.1);border-radius:10px;padding:10px 12px}\n#lv-none:checked~.lv-main .lv-hint{display:block}\n.lv-aside{display:flex;gap:16px;flex:1 1 320px;min-width:280px;align-items:flex-start;flex-wrap:wrap}\n.lv-stage{position:relative;flex:0 0 auto;width:100%;aspect-ratio:9 / 16;background:#000;border-radius:10px;overflow:hidden;box-shadow:0 1px 6px rgba(0,0,0,.2)}\n/* Landscape (wide) screenshot: stack — a large full-width stage on top, with the\n   hint above it and the source tree + panel in a row beneath, all clamped to the\n   stage (screenshot) width and centred, instead of a squeezed side-by-side. */\n.lv-landscape .lv-main{flex-direction:column;align-items:center;flex-wrap:nowrap}\n.lv-landscape .lv-stagecol{width:100%;max-width:min(1000px,96vw)}\n.lv-landscape .lv-stage{max-height:74vh}\n.lv-landscape .lv-aside{width:100%;max-width:min(1000px,96vw);flex:0 0 auto;max-height:none}\n.lv-landscape .lv-tree,.lv-landscape .lv-side{max-height:56vh}\n.lv-shot{position:absolute;inset:0;width:100%;height:100%;object-fit:contain;display:block;user-select:none;-webkit-user-drag:none}\n/* outline-offset:-2px draws highlight borders INSIDE each element box, so\n   adjacent/nested element outlines never overlap or bleed onto neighbours. */\n.lv-el{position:absolute;cursor:pointer;outline:1px solid transparent;outline-offset:-2px}\n.lv-el:hover{outline:2px solid #3b82f6;background:rgba(59,130,246,.14);z-index:3000000}\n.lv-el.lv-xhit{outline:2px solid #a855f7;background:rgba(168,85,247,.22);z-index:2100000}\n/* Ghost overlay for occluded / non-accessibility elements: no overlay normally,\n   a dotted outline only when the element is selected in the source tree. */\n.lv-ghost{position:absolute;display:none;pointer-events:none;outline-offset:-2px}\n.lv-tip{display:none;position:absolute;left:0;bottom:100%;margin-bottom:2px;max-width:260px;padding:2px 6px;border-radius:5px;background:#111;color:#fff;font-size:11px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;pointer-events:none;z-index:6000}\n.lv-el:hover>.lv-tip{display:block}\n.lv-side{flex:1 1 320px;min-width:280px;max-height:78vh;overflow:auto}\n.lv-panel{display:none;background:#fff;border:1px solid rgba(0,0,0,.1);border-radius:10px;padding:12px}\n.lv-panel-head{display:flex;justify-content:space-between;align-items:baseline;gap:8px;margin-bottom:8px}\n.lv-tag{font-weight:600;font-size:14px;word-break:break-all}\n.lv-rect{color:#6e6e73;font-size:12px;white-space:nowrap}\n.lv-sub{margin:12px 0 6px;font-size:11px;text-transform:uppercase;letter-spacing:.04em;color:#8a8a8e}\n.lv-locators{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:6px}\n/* The whole card is the click target (children inherit the pointer and clicks\n   bubble to it), so a click anywhere copies. Feedback is a card-wide overlay,\n   not a small label, so it reads no matter where you clicked. */\n.lv-loc{position:relative;background:#f5f5f7;border-radius:7px;padding:6px 8px;cursor:pointer;outline:1px solid transparent;transition:background .15s,outline-color .15s}\n.lv-loc:hover{outline-color:rgba(59,130,246,.5)}\n.lv-loc:active{background:#e6e6ea}\n.lv-loc::after{content:\"Copied ✓\";position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-weight:600;color:#fff;background:rgba(22,163,74,.94);border-radius:7px;opacity:0;pointer-events:none;transition:opacity .12s}\n.lv-loc.lv-copied::after{opacity:1}\n.lv-loc-head{display:flex;justify-content:space-between;align-items:center;gap:8px}\n.lv-loc-type{font-size:11px;color:#8a8a8e;text-transform:uppercase;letter-spacing:.03em}\n.lv-loc-head::after{content:\"click to copy\";font-size:11px;color:#8a8a8e;opacity:0;transition:opacity .15s;white-space:nowrap}\n.lv-loc:hover .lv-loc-head::after{opacity:.65}\n.lv-loc.lv-copyfail .lv-loc-head::after{content:\"press ⌘/Ctrl+C\";opacity:.65}\n.lv-loc-value{display:block;margin-top:3px;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12px;word-break:break-all}\n/* table-layout:fixed + a fixed first-column width keeps the attribute columns\n   aligned identically across every element, so they do not jump when switching. */\n.lv-attrs{width:100%;border-collapse:collapse;table-layout:fixed}\n.lv-attrs th{width:42%;text-align:left;vertical-align:top;color:#6e6e73;font-weight:500;padding:2px 8px 2px 0;word-break:break-word}\n.lv-attrs td{vertical-align:top;padding:2px 0;word-break:break-all;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12px}\n/* JS-mode selection (the runtime toggles .lv-sel instead of per-node CSS rules). */\n.lv-el.lv-sel{outline:2px solid #e5484d;background:rgba(229,72,77,.16)}\n.lv-ghost.lv-sel{display:block;outline:2px dotted #e5484d;background:rgba(229,72,77,.1);z-index:2000000}\n.lv-node.lv-sel{background:rgba(229,72,77,.18);outline:1px solid rgba(229,72,77,.5)}\n.lv-panel.lv-sel{display:block}\n\n@media (prefers-color-scheme:dark){\nbody{color:#e5e5e7;background:#1c1c1e}\n.lv-panel,.lv-hint{background:#2c2c2e;border-color:rgba(255,255,255,.12)}\n.lv-loc{background:#3a3a3c}\n.lv-loc:active{background:#48484a}\n.lv-head input[type=text],.lv-head .lv-strat{background:#3a3a3c;border-color:rgba(255,255,255,.2)}\n.lv-btn{background:#3a3a3c;border-color:rgba(255,255,255,.2)}\n.lv-btn:hover{background:#48484a}\n#lv-tree-toggle:checked~.lv-head .lv-btn-src{background:#54545a;border-color:rgba(255,255,255,.4)}\n.lv-tree{background:#2c2c2e;border-color:rgba(255,255,255,.12)}\n.lv-tree-head{background:#2c2c2e}\n}\n"

RUNTIME_JS = "\n/* Progressive enhancement — runs only when opened standalone (Allure's inline\n   iframe strips this). Everything above already works without it. */\n(function(){\n  var LV_ROOT=document.querySelector(\"[data-appium-live-view]\");\n  var LV_B64=LV_ROOT?LV_ROOT.getAttribute(\"data-src\"):null;\n  function lvDecodeB64(b){if(!b)return null;var s=atob(b),a=new Uint8Array(s.length);for(var i=0;i<s.length;i++)a[i]=s.charCodeAt(i);return new TextDecoder(\"utf-8\").decode(a);}\n  var LV_XML=lvDecodeB64(LV_B64);\n  function lvBlobFromDataUri(uri){var m=/^data:([^;,]*?)(;base64)?,([\\s\\S]*)$/.exec(uri||\"\");if(!m)return null;var mime=m[1]||\"application/octet-stream\",isB64=!!m[2],data=m[3],bytes;if(isB64){var bin=atob(data);bytes=new Uint8Array(bin.length);for(var i=0;i<bin.length;i++)bytes[i]=bin.charCodeAt(i);}else{bytes=new TextEncoder().encode(decodeURIComponent(data));}return new Blob([bytes],{type:mime});}\n  // XML/Image download controls. Click always downloads; it never navigates —\n  // viewing a blob in a new tab is blocked inside Allure's sandboxed iframe\n  // (blank page), and the Source tree already shows the XML in place. The href +\n  // download attr keep right-click \"Save link as\" working too.\n  function lvWireDownload(el,blob,name){\n    if(!el||!blob)return;\n    var url;try{url=URL.createObjectURL(blob);}catch(e){return;}\n    el.href=url;el.setAttribute(\"download\",name);\n    el.addEventListener(\"click\",function(e){\n      e.preventDefault();\n      var a=document.createElement(\"a\");a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();\n    });\n  }\n  function lvSetLinks(){\n    var ax=document.getElementById(\"lv-xml\");\n    if(ax&&LV_XML){lvWireDownload(ax,new Blob([LV_XML],{type:\"application/xml\"}),\"page-source.xml\");}\n    var ai=document.getElementById(\"lv-img\"),shot=document.querySelector(\".lv-shot\");\n    if(ai&&shot){var blob=lvBlobFromDataUri(shot.getAttribute(\"src\"));if(blob){var ext=/svg/.test(blob.type)?\"svg\":(/(png|jpe?g|webp|gif)/.exec(blob.type)||[0,\"png\"])[1];lvWireDownload(ai,blob,\"screenshot.\"+ext);}}\n  }\n  lvSetLinks();\n  // On selection: the view stays put (radios are position:fixed), and the picked\n  // row is centered in the source tree — scrolling only the tree, never the page.\n  document.addEventListener(\"change\",function(e){\n    if(!e.target||e.target.name!==\"lv-sel\"||e.target.id===\"lv-none\")return;\n    var node=document.querySelector(\".lv-node-\"+e.target.id.replace(\"lv-r-\",\"\"));\n    var tree=document.querySelector(\".lv-tree\");\n    if(node&&tree&&getComputedStyle(tree).display!==\"none\"){\n      var tr=tree.getBoundingClientRect(),nr=node.getBoundingClientRect();\n      tree.scrollTop+=(nr.top-tr.top)-tree.clientHeight/2+nr.height/2;\n    }\n  });\n  function lvFallbackCopy(text){\n    return new Promise(function(resolve,reject){\n      try{\n        var ta=document.createElement(\"textarea\");\n        ta.value=text;ta.setAttribute(\"readonly\",\"\");\n        ta.style.position=\"fixed\";ta.style.top=\"-1000px\";ta.style.opacity=\"0\";\n        document.body.appendChild(ta);ta.select();ta.setSelectionRange(0,text.length);\n        var ok=document.execCommand(\"copy\");document.body.removeChild(ta);\n        ok?resolve():reject(new Error(\"execCommand copy rejected\"));\n      }catch(err){reject(err);}\n    });\n  }\n  function lvCopy(text){\n    if(navigator.clipboard&&navigator.clipboard.writeText){\n      return navigator.clipboard.writeText(text).catch(function(){return lvFallbackCopy(text);});\n    }\n    return lvFallbackCopy(text);\n  }\n  function lvSelect(li){\n    var val=li.querySelector(\".lv-loc-value\");\n    if(!val)return;\n    var r=document.createRange();r.selectNodeContents(val);\n    var s=window.getSelection();s.removeAllRanges();s.addRange(r);\n  }\n  document.addEventListener(\"click\",function(e){\n    var li=e.target.closest(\".lv-loc\");\n    if(!li)return;\n    var val=li.querySelector(\".lv-loc-value\");\n    var v=val?val.textContent:\"\";\n    lvCopy(v).then(function(){\n      li.classList.add(\"lv-copied\");\n      clearTimeout(li.__lvT);\n      li.__lvT=setTimeout(function(){li.classList.remove(\"lv-copied\");},1100);\n    }).catch(function(){\n      // Clipboard blocked — select the value so it can be copied manually; the\n      // hint switches to \"press ⌘/Ctrl+C\" via CSS while the class is set.\n      lvSelect(li);\n      li.classList.add(\"lv-copyfail\");\n      clearTimeout(li.__lvT);\n      li.__lvT=setTimeout(function(){li.classList.remove(\"lv-copyfail\");},1600);\n    });\n  });\n\n  // Locator tester — one input + a strategy dropdown (CSS / XPath). Both evaluate\n  // against an HTML mirror of the source, rooted at the page's <html> for a\n  // <webview> snapshot so the SAME locators shown in the panel (which strip the\n  // wrapper) resolve here too — e.g. an absolute /html/body/… XPath or #id / .class\n  // CSS. Every mirror element carries its node index (data-lvi) for highlighting.\n  var lvDoc=null,lvEls=null;\n  if(LV_XML){\n    try{\n      lvDoc=new DOMParser().parseFromString(LV_XML,\"application/xml\");\n      if(lvDoc.getElementsByTagName(\"parsererror\").length){lvDoc=null;}\n      else{lvEls=lvDoc.getElementsByTagName(\"*\");}\n    }catch(err){lvDoc=null;}\n  }\n  var lvEval=null;\n  if(lvDoc){\n    try{\n      var srcRoot=lvDoc.querySelector(\"html\")||lvDoc.documentElement;\n      var edoc=document.implementation.createHTMLDocument(\"\");\n      var mirror=function(x){var el=edoc.createElement(x.tagName);var k=Array.prototype.indexOf.call(lvEls,x);if(k>=0){el.setAttribute(\"data-lvi\",String(k));}for(var i=0;i<x.attributes.length;i++){var a=x.attributes[i];if(a.name===\"data-lvi\"){continue;}try{el.setAttribute(a.name,a.value);}catch(e){}}for(var c=x.firstElementChild;c;c=c.nextElementSibling){el.appendChild(mirror(c));}return el;};\n      edoc.replaceChild(mirror(srcRoot),edoc.documentElement);\n      lvEval=edoc;\n    }catch(err){lvEval=null;}\n  }\n  var fin=document.getElementById(\"lv-find\"),fstat=document.getElementById(\"lv-fstat\"),fstrat=document.getElementById(\"lv-strat\");\n  function lvStat(t,cls){if(fstat){fstat.textContent=t;fstat.className=\"lv-xpath-status\"+(cls?\" \"+cls:\"\");}}\n  function lvClearHits(){var h=document.querySelectorAll(\".lv-xhit,.lv-node-hit\");for(var i=0;i<h.length;i++){h[i].classList.remove(\"lv-xhit\");h[i].classList.remove(\"lv-node-hit\");}}\n  function lvMark(el){var idx=el&&el.getAttribute?el.getAttribute(\"data-lvi\"):null;if(idx==null){return null;}var tn=document.querySelector(\".lv-node-\"+idx);if(tn){tn.classList.add(\"lv-node-hit\");}var ov=document.querySelector(\".lv-el-\"+idx);if(ov){ov.classList.add(\"lv-xhit\");return ov;}return null;}\n  function lvRunFind(){\n    lvClearHits();\n    var q=fin?fin.value.trim():\"\",strat=fstrat?fstrat.value:\"xpath\";\n    if(!q){lvStat(\"\");return;}\n    if(!lvEval){lvStat(\"source unavailable\",\"err\");return;}\n    var matches=[];\n    if(strat===\"css\"){\n      var res;try{res=lvEval.querySelectorAll(q);}catch(err){lvStat(\"invalid selector\",\"err\");return;}\n      for(var i=0;i<res.length;i++){matches.push(res[i]);}\n    }else{\n      var r;try{r=lvEval.evaluate(q,lvEval,null,7,null);}catch(err){lvStat(\"invalid XPath\",\"err\");return;}\n      for(var j=0;j<r.snapshotLength;j++){matches.push(r.snapshotItem(j));}\n    }\n    var n=matches.length;\n    if(!n){lvStat(\"no match\",\"warn\");return;}\n    var drawn=0,first=null;\n    for(var m=0;m<n;m++){var ov=lvMark(matches[m]);if(ov){drawn++;if(!first){first=ov;}}}\n    if(first){first.scrollIntoView({block:\"nearest\",inline:\"nearest\"});}\n    lvStat(n+\" match\"+(n===1?\"\":\"es\")+(drawn<n?\" · \"+drawn+\" on screen\":\"\"),\"ok\");\n  }\n  if(fin){\n    var ftm;\n    fin.addEventListener(\"input\",function(){clearTimeout(ftm);ftm=setTimeout(lvRunFind,200);});\n    fin.addEventListener(\"keydown\",function(e){if(e.key===\"Enter\"){e.preventDefault();lvRunFind();}});\n  }\n  if(fstrat){fstrat.addEventListener(\"change\",function(){if(fin){fin.placeholder=fstrat.value===\"css\"?\"Test CSS, e.g. input[name=q]\":\"Test XPath, e.g. //*[@text='OK']\";}lvRunFind();});}\n  if(fin&&fin.value){lvRunFind();}\n  document.addEventListener(\"keydown\",function(e){\n    if(e.key===\"Escape\"&&document.activeElement!==fin){var nn=document.getElementById(\"lv-none\");if(nn){nn.checked=true;}}\n  });\n})();\n\n"

# Appended to RUNTIME_JS only in JS mode: rebuilds overlays/tree/panels from the
# embedded source on load (so the shipped HTML can omit them).
RUNTIME_VIEW_JS = "(function lvBuildView() {\n  var root = document.querySelector('[data-appium-live-view][data-lv-js]');\n  if (!root || root.__lvBuilt) return;\n  var b64 = root.getAttribute('data-src');\n  if (!b64) return;\n  var xml;\n  try {\n    var s = atob(b64), u = new Uint8Array(s.length);\n    for (var i = 0; i < s.length; i++) u[i] = s.charCodeAt(i);\n    xml = new TextDecoder('utf-8').decode(u);\n  } catch (e) {\n    return;\n  }\n  var doc;\n  try {\n    doc = new DOMParser().parseFromString(xml, 'application/xml');\n    if (doc.getElementsByTagName('parsererror').length) return;\n  } catch (e) {\n    return;\n  }\n  root.__lvBuilt = true;\n  var els = doc.getElementsByTagName('*');\n  var extM = /(\\d+)x(\\d+)/.exec(root.getAttribute('data-ext') || '') || ['', '9', '16'];\n  var EW = +extM[1], EH = +extM[2];\n  var offM = (root.getAttribute('data-off') || '0,0').split(',');\n  var OX = +offM[0] || 0, OY = +offM[1] || 0;\n  var WEB = root.getAttribute('data-web') === '1';\n  var TIP = ['resource-id', 'content-desc', 'text', 'name', 'label', 'value', 'type', 'class'];\n  var IDENT = /^[A-Za-z_][\\w-]*$/;\n\n  function esc(x) {\n    return String(x).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/\"/g, '&quot;');\n  }\n  function pct(p, w) {\n    return w > 0 ? (p / w * 100).toFixed(4) + '%' : '0%';\n  }\n  function attrsOf(el) {\n    var o = {}, at = el.attributes;\n    for (var i = 0; i < at.length; i++) o[at[i].name] = at[i].value;\n    return o;\n  }\n  function rectOf(a) {\n    if (a.bounds) {\n      var p = a.bounds.split(/[\\[\\],]/).filter(function (x) { return x !== ''; });\n      if (p.length >= 4) {\n        var x1 = +p[0], y1 = +p[1], x2 = +p[2], y2 = +p[3];\n        return { x1: x1, y1: y1, x2: x2, y2: y2, w: x2 - x1, h: y2 - y1 };\n      }\n      return null;\n    }\n    if (a.x != null && a.width != null) {\n      var X = +a.x, Y = +a.y, W = +a.width, H = +a.height;\n      return { x1: X, y1: Y, x2: X + W, y2: Y + H, w: W, h: H };\n    }\n    return null;\n  }\n  function depthOf(el) {\n    var d = 0, p = el.parentElement;\n    while (p) { d++; p = p.parentElement; }\n    return d;\n  }\n  function xlit(v) {\n    if (v.indexOf(\"'\") < 0) return \"'\" + v + \"'\";\n    if (v.indexOf('\"') < 0) return '\"' + v + '\"';\n    return 'concat(' + v.split(\"'\").map(function (p) { return \"'\" + p + \"'\"; }).join(', \"\\'\", ') + ')';\n  }\n  function cssv(v) {\n    return String(v).replace(/\\\\/g, '\\\\\\\\').replace(/\"/g, '\\\\\"');\n  }\n  function absXpath(node) {\n    var segs = [], cur = node;\n    while (cur && cur.parentElement) {\n      var sib = [], c;\n      for (c = cur.parentElement.firstElementChild; c; c = c.nextElementSibling) {\n        if (c.tagName === cur.tagName) sib.push(c);\n      }\n      segs.unshift(sib.length > 1 ? cur.tagName + '[' + (sib.indexOf(cur) + 1) + ']' : cur.tagName);\n      cur = cur.parentElement;\n    }\n    if (cur) segs.unshift(cur.tagName);\n    return '/' + segs.join('/');\n  }\n  function locators(el, a, tag) {\n    var out = [], seen = {};\n    function add(type, val) {\n      if (!val) return;\n      var k = type + '|' + val;\n      if (seen[k]) return;\n      seen[k] = 1;\n      out.push({ type: type, value: val });\n    }\n    if (WEB) {\n      if (a.id) add('css', IDENT.test(a.id) ? '#' + a.id : tag + '[id=\"' + cssv(a.id) + '\"]');\n      if (a.name) add('css', tag + '[name=\"' + cssv(a.name) + '\"]');\n      if (a['aria-label']) add('css', tag + '[aria-label=\"' + cssv(a['aria-label']) + '\"]');\n      if (a['class']) {\n        var cc = a['class'].trim().split(/\\s+/).filter(function (c) { return IDENT.test(c); }).slice(0, 3)\n          .map(function (c) { return '.' + c; }).join('');\n        if (cc) add('css', tag + cc);\n      }\n      if (a.text) add('xpath', '//' + tag + '[normalize-space()=' + xlit(a.text) + ']');\n      add('xpath (absolute)', absXpath(el).replace(/^\\/webview/, ''));\n    } else {\n      add('accessibility id', a['content-desc'] || a.name);\n      add('id', a['resource-id']);\n      if (a['resource-id']) add('xpath', '//*[@resource-id=' + xlit(a['resource-id']) + ']');\n      if (a.text) add('xpath', '//*[@text=' + xlit(a.text) + ']');\n      if (a.label) add('xpath', '//' + tag + '[@label=' + xlit(a.label) + ']');\n      if (a.value) add('xpath', '//' + tag + '[@value=' + xlit(a.value) + ']');\n      add('xpath (absolute)', absXpath(el));\n    }\n    return out;\n  }\n\n  var N = els.length, meta = new Array(N), areas = [];\n  for (var i2 = 0; i2 < N; i2++) {\n    var el = els[i2], a = attrsOf(el), r = rectOf(a);\n    var hasRect = !!(r && r.w > 0 && r.h > 0);\n    var isBox = hasRect && a.visible !== 'false' && a.displayed !== 'false' && a.accessible !== 'false';\n    meta[i2] = { el: el, a: a, r: r, hasRect: hasRect, isBox: isBox, depth: depthOf(el), tag: el.tagName };\n    if (hasRect) areas.push([i2, r.w * r.h]);\n  }\n  areas.sort(function (x, y) { return y[1] - x[1]; });\n  var rank = {};\n  for (var k = 0; k < areas.length; k++) rank[areas[k][0]] = Math.min(k, 999);\n  function zOf(i) {\n    var m = meta[i], r = m.r;\n    var on = r.x1 >= 0 && r.y1 >= 0 && r.x2 <= EW && r.y2 <= EH;\n    return (on ? 1000000 : 0) + m.depth * 1000 + (rank[i] || 0);\n  }\n  function tip(a, tag) {\n    for (var t = 0; t < TIP.length; t++) if (a[TIP[t]]) return tag + ' · ' + a[TIP[t]];\n    return tag;\n  }\n\n  // Overlays into the stage.\n  var ov = '';\n  for (var j = 0; j < N; j++) {\n    var mj = meta[j];\n    if (!mj.hasRect) continue;\n    var rj = mj.r;\n    var st = 'left:' + pct(OX + rj.x1, EW) + ';top:' + pct(OY + rj.y1, EH) +\n      ';width:' + pct(rj.w, EW) + ';height:' + pct(rj.h, EH) + ';z-index:' + zOf(j);\n    if (mj.isBox) {\n      ov += '<label class=\"lv-el lv-el-' + j + '\" data-i=\"' + j + '\" style=\"' + st + '\"><span class=\"lv-tip\">' +\n        esc(tip(mj.a, mj.tag)) + '</span></label>';\n    } else {\n      ov += '<label class=\"lv-ghost lv-el-' + j + '\" data-i=\"' + j + '\" style=\"' + st + '\"></label>';\n    }\n  }\n  var stage = document.querySelector('.lv-stage');\n  if (stage) stage.insertAdjacentHTML('beforeend', ov);\n\n  // Source tree.\n  var tr = '';\n  for (var t2 = 0; t2 < N; t2++) {\n    var mt = meta[t2], at = mt.a;\n    var key = at['resource-id'] || at['content-desc'] || at.text || at.name || at.label || at.value || '';\n    tr += '<div class=\"lv-node lv-node-' + t2 + '\" data-i=\"' + t2 + '\" style=\"padding-left:' + (6 + mt.depth * 13) +\n      'px\" title=\"' + esc(mt.tag) + '\"><span class=\"lv-node-tag\">' + esc(mt.tag) + '</span>' +\n      (key ? ' <span class=\"lv-node-attr\">' + esc(String(key).slice(0, 40)) + '</span>' : '') +\n      (mt.isBox ? '' : ' <span class=\"lv-node-nobox\" title=\"not shown on the screenshot — select here to inspect\">◌</span>') +\n      '</div>';\n  }\n  var tb = document.querySelector('.lv-tree-body');\n  if (tb) tb.insertAdjacentHTML('beforeend', tr);\n\n  // Panels are built on demand.\n  var side = document.querySelector('.lv-side');\n  var built = {};\n  function buildPanel(i) {\n    if (built[i]) return;\n    built[i] = 1;\n    var m = meta[i], a = m.a;\n    var rows = '';\n    for (var kk in a) if (a.hasOwnProperty(kk)) rows += '<tr><th>' + esc(kk) + '</th><td>' + esc(a[kk]) + '</td></tr>';\n    var locs = locators(m.el, a, m.tag).map(function (l) {\n      return '<li class=\"lv-loc\"><div class=\"lv-loc-head\"><span class=\"lv-loc-type\">' + esc(l.type) +\n        '</span></div><code class=\"lv-loc-value\">' + esc(l.value) + '</code></li>';\n    }).join('');\n    var rect = m.r ? '<div class=\"lv-rect\">x ' + m.r.x1 + ', y ' + m.r.y1 + ' · ' + m.r.w + '×' + m.r.h + '</div>' : '';\n    var html = '<div class=\"lv-panel lv-panel-' + i + '\"><div class=\"lv-panel-head\"><span class=\"lv-tag\">' +\n      esc(m.tag) + '</span>' + rect + '</div><div class=\"lv-sub\">Suggested locators</div><ul class=\"lv-locators\">' +\n      locs + '</ul><div class=\"lv-sub\">Attributes</div><table class=\"lv-attrs\">' + rows + '</table></div>';\n    if (side) side.insertAdjacentHTML('beforeend', html);\n  }\n\n  var hint = document.querySelector('.lv-hint');\n  var cur = -1;\n  function select(i) {\n    var was = document.querySelectorAll('.lv-sel');\n    for (var q = 0; q < was.length; q++) was[q].classList.remove('lv-sel');\n    if (i < 0 || i == null) {\n      cur = -1;\n      if (hint) hint.style.display = 'block';\n      return;\n    }\n    cur = i;\n    buildPanel(i);\n    var ovEl = document.querySelector('.lv-el-' + i);\n    if (ovEl) ovEl.classList.add('lv-sel');\n    var tn = document.querySelector('.lv-node-' + i);\n    if (tn) tn.classList.add('lv-sel');\n    var pn = document.querySelector('.lv-panel-' + i);\n    if (pn) pn.classList.add('lv-sel');\n    if (hint) hint.style.display = 'none';\n    var tree = document.querySelector('.lv-tree');\n    if (tn && tree && getComputedStyle(tree).display !== 'none') {\n      var trb = tree.getBoundingClientRect(), nr = tn.getBoundingClientRect();\n      tree.scrollTop += nr.top - trb.top - tree.clientHeight / 2 + nr.height / 2;\n    }\n  }\n\n  document.addEventListener('click', function (e) {\n    var el = e.target.closest ? e.target.closest('[data-i]') : null;\n    if (el) select(+el.getAttribute('data-i'));\n  });\n  document.addEventListener('keydown', function (e) {\n    if (e.key === 'Escape') {\n      var ae = document.activeElement;\n      if (ae && ae.id === 'lv-find') return;\n      select(-1);\n    }\n  });\n\n  var ps = root.getAttribute('data-sel');\n  if (ps != null && ps !== '') select(+ps);\n  else select(-1);\n})();"

# Browser script (run via driver.execute_script in a WebView) that snapshots
# the DOM into the bounds-annotated source the live view consumes.
WEB_SNAPSHOT_JS = "return (function domSnapshot() {\n  var SKIP = { SCRIPT: 1, STYLE: 1, NOSCRIPT: 1, HEAD: 1, META: 1, LINK: 1, TITLE: 1, BASE: 1, TEMPLATE: 1, BR: 1, WBR: 1 };\n  var ATTRS = [\"id\", \"name\", \"type\", \"role\", \"href\", \"src\", \"alt\", \"title\", \"placeholder\", \"aria-label\"];\n  function esc(s) {\n    return String(s).replace(/&/g, \"&amp;\").replace(/</g, \"&lt;\").replace(/>/g, \"&gt;\").replace(/\"/g, \"&quot;\");\n  }\n  function className(el) {\n    var c = el.className;\n    return typeof c === \"string\" ? c : c && c.baseVal ? c.baseVal : \"\";\n  }\n  function ser(el) {\n    if (SKIP[el.tagName]) return \"\";\n    var cs = window.getComputedStyle(el);\n    if (cs.display === \"none\" || cs.visibility === \"hidden\") return \"\";\n    var r = el.getBoundingClientRect();\n    var tag = el.tagName.toLowerCase();\n    var a = \"\";\n    for (var i = 0; i < ATTRS.length; i++) {\n      var v = el.getAttribute(ATTRS[i]);\n      if (v) a += \" \" + ATTRS[i] + '=\"' + esc(v) + '\"';\n    }\n    var cls = className(el).trim();\n    if (cls) a += ' class=\"' + esc(cls) + '\"';\n    if ((tag === \"input\" || tag === \"textarea\" || tag === \"select\") && el.value) a += ' value=\"' + esc(el.value) + '\"';\n    var text = \"\";\n    var ch = el.childNodes;\n    for (var j = 0; j < ch.length; j++) if (ch[j].nodeType === 3) text += ch[j].nodeValue;\n    text = text.replace(/\\s+/g, \" \").trim();\n    if (text) a += ' text=\"' + esc(text.slice(0, 150)) + '\"';\n    a += ' bounds=\"[' + Math.round(r.left) + \",\" + Math.round(r.top) + \"][\" + Math.round(r.right) + \",\" + Math.round(r.bottom) + ']\"';\n    var kids = \"\";\n    for (var k = 0; k < el.children.length; k++) kids += ser(el.children[k]);\n    return \"<\" + tag + a + \">\" + kids + \"</\" + tag + \">\";\n  }\n  var rootEl = document.documentElement; // <html>, so absolute XPaths are /html/body/...\n  var w = Math.round(window.innerWidth);\n  var h = Math.round(window.innerHeight);\n  // On-device geometry the renderer uses to place overlays on the screenshot:\n  //  - dpr: the screenshot is CSS pixels × devicePixelRatio, so the coordinate\n  //    space is taken from the screenshot's own size ÷ dpr (a mobile URL bar can\n  //    make innerHeight here differ from the viewport the screenshot captures).\n  //  - screen: full device size in CSS px. When the screenshot matches this (a\n  //    full-device capture, e.g. iOS Safari with the status bar) rather than the\n  //    web viewport (Android Chrome), the renderer knows to offset overlays by the\n  //    web content's on-screen position instead of assuming it starts at (0,0).\n  var dpr = window.devicePixelRatio || 1;\n  var sw = Math.round(window.screen.width);\n  var sh = Math.round(window.screen.height);\n  return '<?xml version=\"1.0\" encoding=\"UTF-8\"?><webview dpr=\"' + dpr + '\" bounds=\"[0,0][' + w + \",\" + h + ']\" screen=\"[0,0][' + sw + \",\" + sh + ']\">' + ser(rootEl) + \"</webview>\";\n})();"
